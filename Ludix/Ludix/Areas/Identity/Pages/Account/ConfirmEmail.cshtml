@* Areas/Identity/Pages/Account/ConfirmEmail.cshtml *@
@page
@model ConfirmEmailModel
@{
    ViewData["Title"] = "Confirmar email";
}

<h1>@ViewData["Title"]</h1>
<div>
    <p>
        @Model.StatusMessage
    </p>

    @if (Model.StatusMessage == "Obrigado por confirmar o seu email. Já pode fazer login.")
    {
        <p>
            <a asp-page="./Login">Clique aqui para fazer login</a>
        </p>
    }
    else
    {
        <p>
            <a asp-page="./Register">Voltar ao registo</a>
        </p>
    }
</div>
=======
    ViewData["Title"] = "Confirmação de Email";
}

<style>
    :root {
        --primary-dark: #0a192f;
        --secondary-dark: #172a45;
        --accent-gold: #FFD700;
        --text-light: #e6f1ff;
        --success-green: #4CAF50;
    }

    body {
        background-color: var(--primary-dark);
        color: var(--text-light);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 1rem;
    }

    .confirmation-container {
        background-color: var(--secondary-dark);
        border-radius: 10px;
        padding: 3rem;
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        border: 1px solid rgba(76, 175, 80, 0.3);
        max-width: 600px;
        width: 100%;
        text-align: center;
    }

    h1 {
        color: var(--accent-gold);
        font-size: 2.5rem;
        margin-bottom: 2rem;
    }

    .confirmation-icon {
        font-size: 4rem;
        color: var(--success-green);
        margin-bottom: 2rem;
        animation: bounce 1s;
    }

    .status-message {
        background-color: rgba(76, 175, 80, 0.1);
        border-left: 4px solid var(--success-green);
        padding: 1rem;
        margin-bottom: 2rem;
        text-align: left;
        border-radius: 0 4px 4px 0;
    }

    .btn-dashboard {
        background-color: var(--accent-gold);
        border: none;
        color: var(--primary-dark);
        font-weight: 600;
        padding: 0.75rem 2rem;
        border-radius: 30px;
        font-size: 1rem;
        transition: all 0.3s ease;
        cursor: pointer;
        margin-top: 1.5rem;
        display: inline-block;
        text-decoration: none;
    }

        .btn-dashboard:hover {
            background-color: transparent;
            color: var(--accent-gold);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 215, 0, 0.3);
            border: 2px solid var(--accent-gold);
        }

    @@keyframes bounce {
        0%, 20%, 50%, 80%, 100% {
            transform: translateY(0);
        }

        40% {
            transform: translateY(-20px);
        }

        60% {
            transform: translateY(-10px);
        }
    }

    .next-steps {
        margin-top: 2rem;
        padding-top: 1.5rem;
        border-top: 1px solid rgba(255, 215, 0, 0.2);
    }

    .steps-title {
        color: var(--accent-gold);
        font-size: 1.2rem;
        margin-bottom: 1rem;
    }

    .steps-list {
        text-align: left;
        padding-left: 1.5rem;
    }

        .steps-list li {
            margin-bottom: 0.5rem;
        }
</style>

<div class="confirmation-container">
    <div class="confirmation-icon">✓</div>
    <h1>@ViewData["Title"]</h1>

    <div class="status-message">
        <partial name="_StatusMessage" model="Model.StatusMessage" />
    </div>

    <p>Obrigado por confirmar o seu endereço de email. A sua conta está agora ativa e pronta para uso.</p>

    <a href="/" class="btn-dashboard">Ir para o Dashboard</a>

    <div class="next-steps">
        <h3 class="steps-title">Próximos Passos:</h3>
        <ul class="steps-list">
            <li>Complete o seu perfil de utilizador</li>
            <li>Explore os jogos disponíveis</li>
            <li>Adicione jogos à sua wishlist</li>
        </ul>
    </div>
</div>
