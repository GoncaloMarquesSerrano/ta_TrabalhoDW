@page
@model LockoutModel
@{
    ViewData["Title"] = "Conta Bloqueada";
}

<style>
    :root {
        --primary-dark: #0a192f;
        --secondary-dark: #172a45;
        --accent-gold: #FFD700;
        --text-light: #e6f1ff;
        --error-red: #ff6b6b;
    }

    body {
        background-color: var(--primary-dark);
        color: var(--text-light);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 1rem;
    }

    .lockout-container {
        background-color: var(--secondary-dark);
        border-radius: 10px;
        padding: 3rem;
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        border: 1px solid rgba(255, 0, 0, 0.2);
        max-width: 600px;
        width: 100%;
        text-align: center;
    }

    h1 {
        color: var(--error-red);
        font-size: 2.5rem;
        margin-bottom: 1.5rem;
    }

    .lockout-icon {
        font-size: 4rem;
        color: var(--error-red);
        margin-bottom: 1.5rem;
        animation: pulse 2s infinite;
    }

    p {
        color: var(--text-light);
        font-size: 1.2rem;
        line-height: 1.6;
        margin-bottom: 2rem;
    }

    .contact-support {
        margin-top: 2rem;
        padding-top: 1.5rem;
        border-top: 1px solid rgba(255, 0, 0, 0.2);
    }

    .support-link {
        color: var(--accent-gold);
        text-decoration: none;
        transition: all 0.3s ease;
    }

        .support-link:hover {
            text-decoration: underline;
        }

    .btn-home {
        background-color: transparent;
        border: 2px solid var(--accent-gold);
        color: var(--accent-gold);
        font-weight: 600;
        padding: 0.75rem 2rem;
        border-radius: 30px;
        font-size: 1rem;
        transition: all 0.3s ease;
        cursor: pointer;
        margin-top: 1.5rem;
        display: inline-block;
    }

        .btn-home:hover {
            background-color: var(--accent-gold);
            color: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(255, 215, 0, 0.3);
        }

    @@keyframes pulse {
        0% {
            transform: scale(1);
        }

        50% {
            transform: scale(1.1);
        }

        100% {
            transform: scale(1);
        }
    }
</style>

<div class="lockout-container">
    <div class="lockout-icon">🔒</div>
    <h1>@ViewData["Title"]</h1>
    <p>Esta conta foi temporariamente bloqueada devido a várias tentativas de login falhadas.</p>
    <p>Por favor, tente novamente mais tarde ou entre em contacto com o suporte.</p>

    <a href="/" class="btn-home">Voltar à Página Inicial</a>

    <div class="contact-support">
        <p>Precisa de ajuda? <a href="/suporte" class="support-link">Contacte o nosso suporte</a></p>
    </div>
</div>